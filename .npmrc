# Enforce consistent engine/peer behavior
engine-strict=true
strict-peer-dependencies=true

# Minimal, safe hoisting for common typings (helps some tools)
public-hoist-pattern[]=@types/*

# Registry (defaults to npmjs). Uncomment to override.
# registry=https://registry.npmjs.org/
